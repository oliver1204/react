export interface State {
    currentIndex: number,
    flag: boolean
}
export interface Props {
    children?: React.ReactNode,
    tabsClass?: string,
    tabClass?: string,
    initIndex?: number,
    handleClick?: any
}
export interface Data {
    
}